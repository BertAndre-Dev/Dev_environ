"use client";

/**
 * Temporarily disabled — notifications (API + UI).
 * Full implementation preserved below (commented). Re-enable by restoring this
 * page body, uncommenting NotificationsBell in layout, admin nav, and API thunks.
 */
export default function AdminNotificationsPage() {
  return (
    <div className="space-y-2">
      <h1 className="font-heading text-3xl font-bold">Notifications</h1>
      <p className="text-sm text-muted-foreground">
        Notifications are temporarily unavailable.
      </p>
    </div>
  );
}
